import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'worker_assistance_battery',
  webDir: 'www'
};

export default config;
